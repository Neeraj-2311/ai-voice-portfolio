Voice agents that ship to enterprise, not just demos.

## The problem

An enterprise voice AI platform needed to do three things that most off-the-shelf stacks fail on at the same time: hold a natural conversation under one second per stage, isolate tenant data to GDPR standards, and slot into existing telephony and CRM without rewriting either. Demo-quality voice was not the bar. Production was.

## The approach

I architected the backend from zero with the founder, then led a small team of senior engineers through the build. The runtime is a Python orchestration layer on LiveKit handling realtime media and turn-taking, with Azure AI Foundry behind the LLM and retrieval calls. Tool-use and external integrations run on a single shared runtime so context stays coherent across a call.

Multi-tenancy is enforced end to end and verified under load. Onboarding a tenant is automated rather than a manual ops job. Access control sits on top of all of this as an RBAC layer I designed and built independently, so every action across the platform passes through a role and permission check before it reaches data or tools.

## Tech decisions worth noting

- **LiveKit over raw WebRTC.** Realtime primitives and turn-taking are battle-tested, and the deployment model fit the security posture we needed.
- **Azure AI Foundry for the LLM layer.** Aligned with the cloud enterprise buyers already trust, which keeps procurement and security review tractable.
- **Python for the agent runtime.** Strongest agentic and RAG ecosystem, and latency budgets held at the scale we needed.
- **TypeScript and Next.js for the dashboard.** Clean API boundary, faster UI iteration, separate deploy cadence from the agent runtime.
- **Docker everywhere.** Same image, same behaviour, from local to staging to per-tenant production.

## Outcome

- Shipped to enterprise production before contract end.
- Held ~1.2s median full-turn latency end to end (STT, LLM, TTS, VAD combined), with sub-second numbers on individual stages.
- Load tested at roughly 10 concurrent tenants with isolation verified across data, runtime, and deployment.
- Met the enterprise security bar with the multi-tenant model and RBAC layer intact.

## What I learned

The hardest engineering was not the model layer. It was making the orchestration deterministic enough to debug. Voice agents fail in ways text agents do not: silence, partial speech, barge-in mid-tool-call, network jitter that looks like a user hanging up. Off-the-shelf eval tooling does not cover these failure modes yet, so a meaningful chunk of senior engineering time goes into closing that gap. That is what made the difference between a demo and something an enterprise will actually run.
